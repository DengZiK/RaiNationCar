# OpenOCD 配置 - Horco CMSIS-DAP 调试器 + STM32F407
# 使用 HID 后端（此固件在 bulk 后端上不稳定）

adapter driver cmsis-dap
cmsis_dap_vid_pid 0xfaed 0x4870
cmsis_dap_backend hid
transport select swd
adapter speed 4000

source [find target/stm32f4x.cfg]
